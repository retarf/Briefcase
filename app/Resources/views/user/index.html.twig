{% extends 'base.html.twig' %}
{% block body %}
	
	<div class="container">

		<div style="float: right">

			<a href="add" class="btn btn-success"> Dodaj </a>
		</div>
		<div style="clear:both"></div>


		<table class="table table-striped table-hover ">
		  <thead>
		    <tr>
		      <th>#</th>
		      <th>Nazwa użytkownika:</th>
		      <th>Rola</th>
		    </tr>
		  </thead>
		  <tbody>
		  	{% set i = 0 %}
			{% for user in users %}
			    {% set i = i + 1 %}
				    <tr class="active clickable-row" style="cursor: pointer" onclick="getElementById('edit-{{ user.id }}').click()"  >
							<a href="{{ user.id }}" id="edit-{{ user.id }}"><td>{{ i }}</td>

							<td>{{ user.username }}</td>
							<td>{{ user.roles[0] }}</td>
				    </tr>
			{% endfor %}

		  </tbody>
		</table> 	

	</div>

{% endblock %}